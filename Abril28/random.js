function inicio() {
  let numeroUno = prompt("Inserta el numero uno");
  let numeroDos = prompt("Inserta el numero dos");
  let operacion = prompt ("sumar, restar o multiplicar");

if (operacion=="sumar") {
  let resultado = parseInt(numeroUno)+ parseInt(numeroDos);
  alert(resultado);
 if (operacion == "multiplicar") {
    let resultado = parseInt(numeroUno)* parseInt(numeroDos);
    alert (resultado);
 if (operacion=="restar") {
    let resultado =parseInt(numeroUno)- parseInt(numeroDos);
    alert (resultado);
    if {
      alert("ERROR")
      }
    }

      }
    }
}
